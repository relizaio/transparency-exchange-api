{
  "name": "tea-ecmarkup-poc",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">= 20"
  },
  "scripts": {
    "generate-spec": "node build.js",
    "build-only": "ecmarkup --verbose spec.html out/index.html --max-clause-depth 6",
    "build-multipage": "ecmarkup --verbose spec.html --multipage out --max-clause-depth 6",
    "build-for-pdf": "ecmarkup --verbose spec.html out/index.html --assets external --assets-dir out --printable --max-clause-depth 6",
    "build": "npm run generate-spec && npm run build-only",
    "pdf": "npm run generate-spec && mkdir -p out && npm run build-for-pdf && prince-books --script ./node_modules/ecmarkup/js/print.js out/index.html -o out/TEA.pdf",
    "pdf:weasyprint": "npm run generate-spec && mkdir -p out && npm run build-for-pdf && weasyprint out/index.html out/TEA.pdf",
    "clean": "rm -rf out spec.html"
  },
  "dependencies": {
    "@apidevtools/json-schema-ref-parser": "^11.7.0",
    "ecmarkup": "^21.5.0",
    "js-beautify": "^1.15.4",
    "js-yaml": "^4.1.0",
    "markdown-it": "^14.1.0",
    "markdown-it-anchor": "^9.2.0"
  }
}
